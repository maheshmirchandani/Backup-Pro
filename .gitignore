# Binaries (anchored to repo root so cmd/flashbackup/ source files are NOT
# matched; an unanchored "flashbackup" rule also matches the cmd/flashbackup
# subdirectory, hiding the entire package from git tracking).
/flashbackup
/flashbackup-*
*.exe
*.dll
*.so
*.dylib

# Build / test artifacts
*.test
*.out
*.prof
coverage.out
dist/
testdata/benchmarks-*.txt

# Local config
.env
.env.local

# Editor
.idea/
.vscode/
*.swp

# Brainstorm + planning artifacts (not source of truth)
.superpowers/

# macOS Finder
.DS_Store
